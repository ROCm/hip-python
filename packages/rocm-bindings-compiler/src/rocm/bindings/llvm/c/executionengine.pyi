# AUTO-GENERATED by the hip-python code generator.
# Type stubs for rocm.bindings.llvm.c.executionengine. Edits will be overwritten.

import enum
import rocm.bindings.util.types
from typing import Any

def has_symbol(name: str | bytes | bytearray) -> bool: ...

def LLVMLinkInMCJIT():
    r"""Empty function used to force the linker to link MCJIT.

    Has no effect when called on a pre-built library (dylib interface).

    .. rubric:: C signature

    .. code-block:: c

        void LLVMLinkInMCJIT()
    """
    ...

def LLVMLinkInInterpreter():
    r"""Empty function used to force the linker to link the LLVM interpreter.

    Has no effect when called on a pre-built library (dylib interface).

    .. rubric:: C signature

    .. code-block:: c

        void LLVMLinkInInterpreter()
    """
    ...

class LLVMOpaqueGenericValue(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

LLVMGenericValueRef = LLVMOpaqueGenericValue

class LLVMOpaqueExecutionEngine(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

LLVMExecutionEngineRef = LLVMOpaqueExecutionEngine

class LLVMOpaqueMCJITMemoryManager(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

LLVMMCJITMemoryManagerRef = LLVMOpaqueMCJITMemoryManager

class LLVMMCJITCompilerOptions(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...
    OptLevel: Any
    CodeModel: Any
    NoFramePointerElim: Any
    EnableFastISel: Any
    MCJMM: Any
    @staticmethod
    def PROPERTIES() -> list[str]: ...
    @staticmethod
    def fromObj(pyobj) -> Any: ...
    @staticmethod
    def allocate(count: int = 1) -> Any: ...
    def c_sizeof(self) -> int: ...
    def as_c_void_p(self) -> Any: ...
    def __int__(self) -> int: ...
    def __contains__(self, item) -> bool: ...
    def __getitem__(self, item: int) -> Any: ...

def LLVMCreateGenericValueOfInt(Ty, N, IsSigned):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        Ty (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        N (:py:obj:`~.int`):
            (undocumented)

        IsSigned (:py:obj:`~.int`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMGenericValueRef LLVMCreateGenericValueOfInt(LLVMTypeRef Ty, unsigned long long N, LLVMBool IsSigned)
    """
    ...

def LLVMCreateGenericValueOfPointer(P):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        P (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMGenericValueRef LLVMCreateGenericValueOfPointer(void * P)
    """
    ...

def LLVMCreateGenericValueOfFloat(Ty, N):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        Ty (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        N (:py:obj:`~.float`/:py:obj:`~.int`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMGenericValueRef LLVMCreateGenericValueOfFloat(LLVMTypeRef Ty, double N)
    """
    ...

def LLVMGenericValueIntWidth(GenValRef):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        GenValRef (:py:obj:`~.LLVMOpaqueGenericValue`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.int`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        unsigned int LLVMGenericValueIntWidth(LLVMGenericValueRef GenValRef)
    """
    ...

def LLVMGenericValueToInt(GenVal, IsSigned):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        GenVal (:py:obj:`~.LLVMOpaqueGenericValue`/:py:obj:`~.object`):
            (undocumented)

        IsSigned (:py:obj:`~.int`):
            (undocumented)

    Returns:
        :py:obj:`~.int`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        unsigned long long LLVMGenericValueToInt(LLVMGenericValueRef GenVal, LLVMBool IsSigned)
    """
    ...

def LLVMGenericValueToPointer(GenVal):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        GenVal (:py:obj:`~.LLVMOpaqueGenericValue`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void * LLVMGenericValueToPointer(LLVMGenericValueRef GenVal)
    """
    ...

def LLVMGenericValueToFloat(TyRef, GenVal):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        TyRef (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        GenVal (:py:obj:`~.LLVMOpaqueGenericValue`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.float`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        double LLVMGenericValueToFloat(LLVMTypeRef TyRef, LLVMGenericValueRef GenVal)
    """
    ...

def LLVMDisposeGenericValue(GenVal):
    r"""===-- Operations on generic values --------------------------------------===

    Args:
        GenVal (:py:obj:`~.LLVMOpaqueGenericValue`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMDisposeGenericValue(LLVMGenericValueRef GenVal)
    """
    ...

def LLVMCreateExecutionEngineForModule(M):
    r"""===-- Operations on execution engines -----------------------------------===

    Args:
        M (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 3 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutEE (:py:obj:`~.LLVMOpaqueExecutionEngine`):
            (undocumented)
        * OutError (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMCreateExecutionEngineForModule(LLVMExecutionEngineRef * OutEE, LLVMModuleRef M, char ** OutError)
    """
    ...

def LLVMCreateInterpreterForModule(M):
    r"""===-- Operations on execution engines -----------------------------------===

    Args:
        M (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 3 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutInterp (:py:obj:`~.LLVMOpaqueExecutionEngine`):
            (undocumented)
        * OutError (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMCreateInterpreterForModule(LLVMExecutionEngineRef * OutInterp, LLVMModuleRef M, char ** OutError)
    """
    ...

def LLVMCreateJITCompilerForModule(M, OptLevel):
    r"""===-- Operations on execution engines -----------------------------------===

    Args:
        M (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        OptLevel (:py:obj:`~.int`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 3 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutJIT (:py:obj:`~.LLVMOpaqueExecutionEngine`):
            (undocumented)
        * OutError (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMCreateJITCompilerForModule(LLVMExecutionEngineRef * OutJIT, LLVMModuleRef M, unsigned int OptLevel, char ** OutError)
    """
    ...

def LLVMInitializeMCJITCompilerOptions(Options, SizeOfOptions):
    r"""===-- Operations on execution engines -----------------------------------===

    Args:
        Options (:py:obj:`~.LLVMMCJITCompilerOptions`/:py:obj:`~.object`):
            (undocumented)

        SizeOfOptions (:py:obj:`~.int`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMInitializeMCJITCompilerOptions(struct LLVMMCJITCompilerOptions * Options, size_t SizeOfOptions)
    """
    ...

def LLVMCreateMCJITCompilerForModule(M, Options, SizeOfOptions):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        M (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        Options (:py:obj:`~.LLVMMCJITCompilerOptions`/:py:obj:`~.object`):
            (undocumented)

        SizeOfOptions (:py:obj:`~.int`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 3 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutJIT (:py:obj:`~.LLVMOpaqueExecutionEngine`):
            (undocumented)
        * OutError (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMCreateMCJITCompilerForModule(LLVMExecutionEngineRef * OutJIT, LLVMModuleRef M, struct LLVMMCJITCompilerOptions * Options, size_t SizeOfOptions, char ** OutError)
    """
    ...

def LLVMDisposeExecutionEngine(EE):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMDisposeExecutionEngine(LLVMExecutionEngineRef EE)
    """
    ...

def LLVMRunStaticConstructors(EE):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMRunStaticConstructors(LLVMExecutionEngineRef EE)
    """
    ...

def LLVMRunStaticDestructors(EE):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMRunStaticDestructors(LLVMExecutionEngineRef EE)
    """
    ...

def LLVMRunFunctionAsMain(EE, F, ArgC, ArgV, EnvP):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        F (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        ArgC (:py:obj:`~.int`):
            (undocumented)

        ArgV (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        EnvP (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.int`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        int LLVMRunFunctionAsMain(LLVMExecutionEngineRef EE, LLVMValueRef F, unsigned int ArgC, const char *const * ArgV, const char *const * EnvP)
    """
    ...

def LLVMRunFunction(EE, F, NumArgs, Args):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        F (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        NumArgs (:py:obj:`~.int`):
            (undocumented)

        Args (:py:obj:`~.rocm.bindings.util.types.ListOfPointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMGenericValueRef LLVMRunFunction(LLVMExecutionEngineRef EE, LLVMValueRef F, unsigned int NumArgs, LLVMGenericValueRef * Args)
    """
    ...

def LLVMFreeMachineCodeForFunction(EE, F):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        F (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMFreeMachineCodeForFunction(LLVMExecutionEngineRef EE, LLVMValueRef F)
    """
    ...

def LLVMAddModule(EE, M):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        M (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMAddModule(LLVMExecutionEngineRef EE, LLVMModuleRef M)
    """
    ...

def LLVMRemoveModule(EE, M):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        M (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 3 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutMod (:py:obj:`~.LLVMOpaqueModule`):
            (undocumented)
        * OutError (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMRemoveModule(LLVMExecutionEngineRef EE, LLVMModuleRef M, LLVMModuleRef * OutMod, char ** OutError)
    """
    ...

def LLVMFindFunction(EE, Name):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        Name (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 2 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutFn (:py:obj:`~.LLVMOpaqueValue`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMFindFunction(LLVMExecutionEngineRef EE, const char * Name, LLVMValueRef * OutFn)
    """
    ...

def LLVMRecompileAndRelinkFunction(EE, Fn):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        Fn (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void * LLVMRecompileAndRelinkFunction(LLVMExecutionEngineRef EE, LLVMValueRef Fn)
    """
    ...

def LLVMGetExecutionEngineTargetData(EE):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMTargetDataRef LLVMGetExecutionEngineTargetData(LLVMExecutionEngineRef EE)
    """
    ...

def LLVMGetExecutionEngineTargetMachine(EE):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMTargetMachineRef LLVMGetExecutionEngineTargetMachine(LLVMExecutionEngineRef EE)
    """
    ...

def LLVMAddGlobalMapping(EE, Global, Addr):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        Global (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

        Addr (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMAddGlobalMapping(LLVMExecutionEngineRef EE, LLVMValueRef Global, void * Addr)
    """
    ...

def LLVMGetPointerToGlobal(EE, Global):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        Global (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void * LLVMGetPointerToGlobal(LLVMExecutionEngineRef EE, LLVMValueRef Global)
    """
    ...

def LLVMGetGlobalValueAddress(EE, Name):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        Name (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.int`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        uint64_t LLVMGetGlobalValueAddress(LLVMExecutionEngineRef EE, const char * Name)
    """
    ...

def LLVMGetFunctionAddress(EE, Name):
    r"""Create an MCJIT execution engine for a module, with the given options.

    It is
    the responsibility of the caller to ensure that all fields in Options up to
    the given SizeOfOptions are initialized. It is correct to pass a smaller
    value of SizeOfOptions that omits some fields. The canonical way of using
    this is:

    LLVMMCJITCompilerOptions options;
    LLVMInitializeMCJITCompilerOptions(&options, sizeof(options));
    ... fill in those options you care about
    LLVMCreateMCJITCompilerForModule(&jit, mod, &options, sizeof(options),
                                     &error);

    Note that this is also correct, though possibly suboptimal:

    LLVMCreateMCJITCompilerForModule(&jit, mod, 0, 0, &error);

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

        Name (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.int`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        uint64_t LLVMGetFunctionAddress(LLVMExecutionEngineRef EE, const char * Name)
    """
    ...

def LLVMExecutionEngineGetErrMsg(EE):
    r"""Returns true on error, false on success.

    If true is returned then the error
    message is copied to OutStr and cleared in the ExecutionEngine instance.

    Args:
        EE (:py:obj:`~.LLVMOpaqueExecutionEngine`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        A :py:obj:`~.tuple` of size 2 that contains (in that order):

        * :py:obj:`~.int`: (undocumented)
        * OutError (:py:obj:`~.rocm.bindings.util.types.CStr`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMBool LLVMExecutionEngineGetErrMsg(LLVMExecutionEngineRef EE, char ** OutError)
    """
    ...

class LLVMMemoryManagerAllocateCodeSectionCallback(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

class LLVMMemoryManagerAllocateDataSectionCallback(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

class LLVMMemoryManagerFinalizeMemoryCallback(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

class LLVMMemoryManagerDestroyCallback(rocm.bindings.util.types.Pointer):
    r"""(No short description)
    """
    def __init__(self, *args, **kwargs): ...

def LLVMCreateSimpleMCJITMemoryManager(Opaque, AllocateCodeSection, AllocateDataSection, FinalizeMemory, Destroy):
    r"""Create a simple custom MCJIT memory manager.

    This memory manager can
    intercept allocations in a module-oblivious way. This will return NULL
    if any of the passed functions are NULL.

    Args:
        Opaque (:py:obj:`~.rocm.bindings.util.types.Pointer`/:py:obj:`~.object`):
            An opaque client object to pass back to the callbacks.

        AllocateCodeSection (:py:obj:`~.LLVMMemoryManagerAllocateCodeSectionCallback`/:py:obj:`~.object`):
            Allocate a block of memory for executable code.

        AllocateDataSection (:py:obj:`~.LLVMMemoryManagerAllocateDataSectionCallback`/:py:obj:`~.object`):
            Allocate a block of memory for data.

        FinalizeMemory (:py:obj:`~.LLVMMemoryManagerFinalizeMemoryCallback`/:py:obj:`~.object`):
            Set page permissions and flush cache. Return 0 on
            success, 1 on error.

        Destroy (:py:obj:`~.LLVMMemoryManagerDestroyCallback`/:py:obj:`~.object`):
            (undocumented)

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMMCJITMemoryManagerRef LLVMCreateSimpleMCJITMemoryManager(void * Opaque, LLVMMemoryManagerAllocateCodeSectionCallback AllocateCodeSection, LLVMMemoryManagerAllocateDataSectionCallback AllocateDataSection, LLVMMemoryManagerFinalizeMemoryCallback FinalizeMemory, LLVMMemoryManagerDestroyCallback Destroy)
    """
    ...

def LLVMDisposeMCJITMemoryManager(MM):
    r"""Create a simple custom MCJIT memory manager.

    This memory manager can
    intercept allocations in a module-oblivious way. This will return NULL
    if any of the passed functions are NULL.

    Args:
        MM (:py:obj:`~.LLVMOpaqueMCJITMemoryManager`/:py:obj:`~.object`):
            (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        void LLVMDisposeMCJITMemoryManager(LLVMMCJITMemoryManagerRef MM)
    """
    ...

def LLVMCreateGDBRegistrationListener():
    r"""===-- JIT Event Listener functions -------------------------------------===

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMJITEventListenerRef LLVMCreateGDBRegistrationListener()
    """
    ...

def LLVMCreateIntelJITEventListener():
    r"""===-- JIT Event Listener functions -------------------------------------===

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMJITEventListenerRef LLVMCreateIntelJITEventListener()
    """
    ...

def LLVMCreateOProfileJITEventListener():
    r"""===-- JIT Event Listener functions -------------------------------------===

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMJITEventListenerRef LLVMCreateOProfileJITEventListener()
    """
    ...

def LLVMCreatePerfJITEventListener():
    r"""===-- JIT Event Listener functions -------------------------------------===

    Returns:
        :py:obj:`~.None`: (undocumented)

    .. rubric:: C signature

    .. code-block:: c

        LLVMJITEventListenerRef LLVMCreatePerfJITEventListener()
    """
    ...

__all__ = [
    'LLVMAddGlobalMapping',
    'LLVMAddModule',
    'LLVMCreateExecutionEngineForModule',
    'LLVMCreateGDBRegistrationListener',
    'LLVMCreateGenericValueOfFloat',
    'LLVMCreateGenericValueOfInt',
    'LLVMCreateGenericValueOfPointer',
    'LLVMCreateIntelJITEventListener',
    'LLVMCreateInterpreterForModule',
    'LLVMCreateJITCompilerForModule',
    'LLVMCreateMCJITCompilerForModule',
    'LLVMCreateOProfileJITEventListener',
    'LLVMCreatePerfJITEventListener',
    'LLVMCreateSimpleMCJITMemoryManager',
    'LLVMDisposeExecutionEngine',
    'LLVMDisposeGenericValue',
    'LLVMDisposeMCJITMemoryManager',
    'LLVMExecutionEngineGetErrMsg',
    'LLVMExecutionEngineRef',
    'LLVMFindFunction',
    'LLVMFreeMachineCodeForFunction',
    'LLVMGenericValueIntWidth',
    'LLVMGenericValueRef',
    'LLVMGenericValueToFloat',
    'LLVMGenericValueToInt',
    'LLVMGenericValueToPointer',
    'LLVMGetExecutionEngineTargetData',
    'LLVMGetExecutionEngineTargetMachine',
    'LLVMGetFunctionAddress',
    'LLVMGetGlobalValueAddress',
    'LLVMGetPointerToGlobal',
    'LLVMInitializeMCJITCompilerOptions',
    'LLVMLinkInInterpreter',
    'LLVMLinkInMCJIT',
    'LLVMMCJITCompilerOptions',
    'LLVMMCJITMemoryManagerRef',
    'LLVMMemoryManagerAllocateCodeSectionCallback',
    'LLVMMemoryManagerAllocateDataSectionCallback',
    'LLVMMemoryManagerDestroyCallback',
    'LLVMMemoryManagerFinalizeMemoryCallback',
    'LLVMOpaqueExecutionEngine',
    'LLVMOpaqueGenericValue',
    'LLVMOpaqueMCJITMemoryManager',
    'LLVMRecompileAndRelinkFunction',
    'LLVMRemoveModule',
    'LLVMRunFunction',
    'LLVMRunFunctionAsMain',
    'LLVMRunStaticConstructors',
    'LLVMRunStaticDestructors',
    'has_symbol',
]
