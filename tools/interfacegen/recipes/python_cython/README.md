# Python interfaces via Cython

This folder contains recipes for generating Python interfaces for
a select list of libraries via the Cython programming language.

## `\_utils` folder

The folder `_util` contains routine and type definitions that are used by the libraries present in this folder. The content of this folder is supposed to be copied into a directory structure created by the code generation recipes.
